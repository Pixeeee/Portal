import { useState } from 'react';import { router } from 'expo-router';import { Screen,Label,Title,Input,Button,Body } from '@/components/ui';import { usePortal } from '@/state/PortalProvider';
export default function EditPortal(){const {place,api,refreshPlace}=usePortal();const [name,setName]=useState(place?.name||'');const [location,setLocation]=useState(place?.location||'');const [description,setDescription]=useState(place?.description||'');const [busy,setBusy]=useState(false);async function save(){if(!api||!name.trim())return;setBusy(true);await api.updatePlace({name:name.trim(),location:location.trim(),description:description.trim()});await refreshPlace();router.back()}return <Screen><Label>Settings</Label><Title>Edit Portal</Title><Input value={name} onChangeText={setName}/><Input value={location} onChangeText={setLocation}/><Input multiline value={description} onChangeText={setDescription}/><Body muted>The public code stays the same when you rename a Portal.</Body><Button title={busy?'Saving…':'Save'} disabled={busy||!name.trim()} onPress={()=>void save()}/></Screen>}
