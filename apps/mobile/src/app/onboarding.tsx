import { useState } from 'react';
import { router } from 'expo-router';
import { Screen, Title, Body, Label, Input, Button } from '@/components/ui';
import { usePortal } from '@/state/PortalProvider';
export default function Onboarding(){const {createPlace}=usePortal();const [name,setName]=useState('');const [location,setLocation]=useState('');const [description,setDescription]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(){if(!name.trim())return;setBusy(true);setError('');try{await createPlace({name:name.trim(),location:location.trim()||undefined,description:description.trim()||undefined});router.replace('/(tabs)');}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <Screen><Label>Portal</Label><Title>Create your Portal</Title><Body muted>Every installation represents one physical place. No employee account or password is required.</Body><Label>Place name *</Label><Input value={name} onChangeText={setName} placeholder="Davao Farm" maxLength={120}/><Label>Location</Label><Input value={location} onChangeText={setLocation} placeholder="Davao City" maxLength={160}/><Label>Description</Label><Input value={description} onChangeText={setDescription} placeholder="Farm Operations" maxLength={1000} multiline/>{error?<Body>{error}</Body>:null}<Button title={busy?'Creating…':'Create Portal'} disabled={busy||!name.trim()} onPress={()=>void submit()}/></Screen>}
