import { View,StyleSheet,Text } from 'react-native';import QRCode from 'react-native-qrcode-svg';import { portalQrPayload } from '@portal/contracts';import { usePortal } from '@/state/PortalProvider';import { colors,Label,Title,Body } from '@/components/ui';
export default function QR(){const {place}=usePortal();if(!place)return null;return <View style={s.root}><Label>Your Portal</Label><Title>{place.name}</Title><View style={s.qr}><QRCode value={portalQrPayload(place.publicCode)} size={260}/></View><Text style={s.code}>{place.publicCode}</Text><Body muted>Scanning resolves this Portal first. It does not automatically open the camera or start a call.</Body></View>}const s=StyleSheet.create({root:{flex:1,backgroundColor:colors.bg,padding:24,justifyContent:'center',gap:18},qr:{backgroundColor:'#fff',padding:22,borderRadius:22,alignSelf:'center'},code:{color:colors.text,textAlign:'center',fontSize:32,fontWeight:'800',letterSpacing:2}});
